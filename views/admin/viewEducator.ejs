<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Educator Details</title>
  </head>
  <body>
    <h1>Educator Details</h1>
    <p><strong>Name:</strong> <%= educator.name %></p>
    <p><strong>Email:</strong> <%= educator.email %></p>
    <p><strong>Phone:</strong> <%= educator.phoneNo %></p>
    <p><strong>Experience:</strong> <%= educator.experienceYears %> years</p>
    <p><strong>Bio:</strong> <%= educator.bio %></p>
    <p><strong>Qualifications:</strong> <%= educator.qualifications %></p>

    <% if (educator.profileImage) { %>
    <p><strong>Profile Image:</strong></p>
    <img
      src="<%= educator.profileImage %>"
      alt="Educator Image"
      width="150px"
    />
    <% } %>

    <h3>Programs:</h3>
    <ul>
      <% educator.programs.forEach(program => { %>
      <li><%= program.programName %></li>
      <% }) %>
    </ul>

    <a href="/admin/educators/edit/<%= educator._id %>">Edit</a>
    <form
      action="/admin/educators/delete/<%= educator._id %>"
      method="POST"
      onsubmit="return confirm('Are you sure you want to delete this educator?');"
    >
      <button type="submit">Delete</button>
    </form>
  </body>
</html>
