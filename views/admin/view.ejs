<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>View Student</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div class="dashboard-container">
      <main class="main-content">
        <h1>Student Details</h1>
        <%= console.log("Profile Image Path:", student.profileImage) %>

        <!-- ✅ Profile Image (Only if available) -->
        <% if (student.profileImage && student.profileImage.trim() !== "") { %>
        <div class="profile-image-container">
          <img
            src="<%= student.profileImage %>"
            alt="Profile Image"
            class="profile-image"
          />
        </div>
        <% } %>

        <table>
          <tr>
            <th>Student ID</th>
            <td><%= student.studentId %></td>
          </tr>
          <tr>
            <th>Name</th>
            <td><%= student.name %></td>
          </tr>
          <tr>
            <th>Status</th>
            <td><%= student.status %></td>
          </tr>
          <tr>
            <th>Program</th>
            <td><%= student.programId.programName %></td>
          </tr>
          <tr>
            <th>Days of Week</th>
            <td><%= student.daysOfWeek.join(", ") %></td>
          </tr>
          <tr>
            <th>Primary Educator</th>
            <td><%= student.educator.name %></td>
          </tr>
          <tr>
            <th>Secondary Educator</th>
            <td>
              <%= student.secondaryEducator ? student.secondaryEducator.name :
              "None" %>
            </td>
          </tr>
          <tr>
            <th>Session Type</th>
            <td><%= student.sessionType %></td>
          </tr>
          <tr>
            <th>Father's Name</th>
            <td><%= student.fatherName %></td>
          </tr>
          <tr>
            <th>Mother's Name</th>
            <td><%= student.motherName %></td>
          </tr>
          <tr>
            <th>Blood Group</th>
            <td><%= student.bloodGroup %></td>
          </tr>
          <tr>
            <th>Contact Number</th>
            <td><%= student.contactNo %></td>
          </tr>
          <tr>
            <th>Alternative Contact</th>
            <td><%= student.alternativeContactNo %></td>
          </tr>
          <tr>
            <th>Parent's Email</th>
            <td><%= student.parentEmail %></td>
          </tr>
          <tr>
            <th>Address</th>
            <td><%= student.address %></td>
          </tr>
          <tr>
            <th>Transport</th>
            <td><%= student.transport %></td>
          </tr>
          <tr>
            <th>Comments</th>
            <td><%= student.comments %></td>
          </tr>
          <tr>
            <th>Enrollment Year</th>
            <td><%= student.enrollmentYear %></td>
          </tr>
        </table>
        <button onclick="window.history.back()">Back</button>
      </main>
    </div>

    <style>
      .profile-image-container {
        text-align: center;
        margin-bottom: 20px;
      }

      .profile-image {
        width: 150px;
        height: 150px;
        border-radius: 50%;
        object-fit: cover;
        border: 3px solid #ddd;
      }
    </style>
  </body>
</html>
